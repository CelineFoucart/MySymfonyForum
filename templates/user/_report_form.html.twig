<article class="rounded-3 bg-default my-2 p-2">
    <h3 class="text-uppercase h6 border-bottom border-dark">Votre rapport</h3>
    {{ form_start(form) }}
        {{ form_rest(form) }}
        <div class="text-center">
            <button type="submit" class="btn btn-primary mt-2">Envoyer</button>
        </div>
    {{ form_end(form) }}
</article>